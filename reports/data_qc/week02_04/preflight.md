# Week 2–4 preflight

- Outcome: `OK`.
- The private metadata root exists and is covered by the repository `data/**` ignore rule.
- Tracked, staged, and historical private-metadata counts are all zero.
- No credential-name indicator or imaging volume was found.
- The user explicitly waived the low-memory blocker. This waiver authorizes metadata-only CPU work, not MRI processing or GPU work.
- HCP Open Access is approved; HCP Restricted Access is not approved. The implementation uses only the approved open-access inputs.
- PPMI metadata access is approved.
- Input inventory at preflight: 43,719,269 bytes across CSV, JSON, Markdown, and PDF files.
