# Remaining user actions

No scientific target decision remains open for Week 2–4.

- HCP: `CogFluidComp_Unadj` primary; `CogTotalComp_Unadj` and `PMAT24_A_CR`
  secondary; regression with MAE.
- PPMI: candidate A primary, defined as MDS-UPDRS Part III follow-up minus
  baseline; candidate B secondary/sensitivity; `prefer_off` primary policy;
  `unique_only` and `prefer_on` sensitivity policies.

Non-blocking limitations remain:

- HCP Restricted Access is not approved and was not used.
- The primary PPMI 24-month branch has 151 independent subjects, so the
  preregistered gate recommends a shorter window.
- PPMI clinical dates are month-year only; exact imaging-clinical day intervals
  are unavailable.

These are scientific/data limitations, not engineering WARN or FAIL states.
